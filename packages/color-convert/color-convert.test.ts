import assert from 'node:assert'
import colors from './color-name'

describe('', ()=>{
	it('', ()=>{
		expect(colors.red).toEqual([255, 0, 0])
		expect(colors.aliceblue).toEqual([240, 248, 255])
	})

})
